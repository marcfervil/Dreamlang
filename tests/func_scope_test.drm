
f = "wow"




func z(param){
    y = "e"

    check("outer scoped var 'x' undefined", "x" in self, false)
    check("outer scoped var 'y' undefined", "c" in self, false)
    check("outer scoped function param", param, 30)
}

func zz(param){
    check("outer scoped function upsetted param", param, 68)
    check("outer scoped function outer scoped var", f, "yup")
}

func a(){
    func b(){
        c = "wowow"
        check("nested function outer scope var defined", x, 30)
        z(x)
        x = 68
        check("nested function upget", f, "wow")
        f = "yup"
    }
    x = 30
    b()
    check("nested function scope undefined", "c" in self, false)
    zz(x)
}
a()
check("inner function scope", "x" in self, false)
check("upsetted from nested", f, "yup")

