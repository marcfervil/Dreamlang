class count {
    value = 0
    max = $
    func init(max)

    func next(first){
        if first -> value = -1
        value = value + 1
    }

    func has_next() -> return value is not max
    func get_next() -> return value
}

for i in count(10) -> print(i)

