class count {

    value = 0
    max = $

    func init(max)

    func next(first){
        if first -> value = -1
        value = value + 1
    }

    func has_next() => value is not max

    func get_next() => value
}


x = count(30)


for num in count(30){
    print(num)
}

